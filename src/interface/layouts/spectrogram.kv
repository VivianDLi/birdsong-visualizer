#:kivy 2.1

<Spectrogram>:
    canvas:
        Color:
            rgb: (1, 0, 0, 1)
        Line:
            #points: (self.offset * self.size[0] / self.shape[0], 0, self.offset * self.size[0] / self.shape[0], self.height)
            points: (0, 0, 0, self.height)
